# CHART RIDER（仮） 詳細ゲーム仕様書

- **和題候補**: チャートを駆けろ！
- **英題候補**: CHART RIDER
- **バージョン**: v0.1
- **対象**: スマートフォン向けブラウザゲーム
- **画面方向**: 縦持ち
- **ジャンル**: ワンタップ・エンドレス・スコアアタック
- **想定技術**: HTML / CSS / JavaScript / Canvas 2D
- **想定公開方法**: GitHub Pages などの静的ホスティング
- **プレイ人数**: 1人
- **基本コンセプト**: タップで上昇、何もしないと下降。激しく上下する「株価チャートの通路」からはみ出さず、できるだけ長く相場についていく。

> 本書中のゲームバランス数値は初期実装用の暫定値。  
> 数値は `CONFIG` に集約し、プレイテスト後に容易に調整できる構造とする。

---

# 1. ゲームコンセプト

Flappy Bird のように、

- 操作説明が数秒で理解できる
- 片手・1本指だけで遊べる
- 電車待ちなどの短時間でも成立する
- 常に画面へ集中する必要がある
- 失敗理由が視覚的に分かる
- リトライまでが非常に短い
- 徐々に難易度が上がる
- 記録更新を狙って何度も遊びたくなる

というミニゲームを目指す。

株要素は知識を要求せず、

**「相場の値動きについていく」**

というゲーム体験そのものに組み込む。

---

# 2. 一言ルール

```text
タップで上昇。
何もしないと落下。
チャートの通路からはみ出したら終了。
```

初見プレイヤーがこれだけで遊べることを最優先とする。

---

# 3. 画面構成

スマートフォン縦持ち、基準アスペクト比は **9:16**。

プレイヤーは画面左寄りに固定し、チャートが右から左へ流れる。

```text
┌────────────────────┐
│ DISTANCE  428m   BEST 812m │
│                           │
│            ╭────╮         │
│       ╭────╯    ╰──╮      │
│     ╭─╯             ╰─╮   │
│   🐶                    │   │
│     ╰─╮             ╭─╯   │
│       ╰────╮    ╭──╯      │
│            ╰────╯         │
│                           │
└────────────────────┘
```

- プレイヤーX座標: 画面幅の約24%
- プレイヤーは上下方向のみ移動
- ワールドは一定速度で左へスクロール
- 上下のチャート境界が実際の当たり判定
- 背景にはローソク足・グリッド・価格表示などを薄く描画可能

---

# 4. 基本操作

## 4.1 タップ

画面を1回タップすると、プレイヤーに上向きの瞬間速度を与える。

```text
TAP → 上向きインパルス
```

押しっぱなしでは連続上昇しない。

Flappy Bird と同様に、上昇したい場合は繰り返しタップする。

## 4.2 無入力

常時重力が働き、プレイヤーは徐々に落下する。

```text
NO INPUT → FALL
```

## 4.3 操作領域

ゲーム中は画面全体をタップ領域とする。

UIボタンを除き、どこをタップしても同じ操作になる。

---

# 5. プレイヤー挙動

プレイヤーは以下の物理量を持つ。

- `y`
- `velocityY`
- `gravity`
- `tapImpulse`

基本更新:

```text
velocityY += gravity * deltaTime
y += velocityY * deltaTime
```

タップ時:

```text
velocityY = tapImpulse
```

タップ時に加算ではなく **上昇速度を一定値へ上書き** する。

これにより連打による異常な加速を防ぎ、操作感を安定させる。

---

# 6. チャート通路

## 6.1 基本構造

チャートは一本の中心線 `centerY` と、その上下に一定幅の通路を持つ。

```text
upperY = centerY - corridorHalfHeight
lowerY = centerY + corridorHalfHeight
```

プレイヤーは、

```text
upperY ～ lowerY
```

の範囲内を飛び続ける。

## 6.2 見た目

通路は「相場の値動き」を表す。

推奨表現:

- 中心線: 株価ライン
- 通路上下: 発光ラインまたは帯
- 通路外: 少し暗くする
- 背景: ローソク足
- 上昇局面: 軽い上昇演出
- 暴落局面: 背景やエフェクトのみ強調

ローソク足そのものには当たり判定を持たせない。

**ゲームとして見るべきものは上下の通路境界だけ** とする。

---

# 7. 当たり判定

プレイヤーの当たり判定は見た目よりやや小さくする。

推奨:

```text
表示サイズに対して 75～85%
```

以下のどちらかに触れた時点でGAME OVER。

```text
playerTop <= upperWall
playerBottom >= lowerWall
```

見た目ではギリギリ接触していても助かる程度の、プレイヤー有利判定を採用する。

「見た目では当たっていないのに死ぬ」状態は禁止。

---

# 8. チャート生成

チャートは実在の株価データではなく、ゲーム用に疑似生成する。

## 8.1 セグメント方式

一定距離ごとに次の目標Y座標を生成し、現在位置との間を滑らかな曲線で接続する。

候補:

- 緩やかな上昇
- 緩やかな下降
- 横ばい
- 山型
- 谷型
- 急騰
- 急落
- 細かな上下動

接続には `smoothstep`、Bezier、Catmull-Rom などを使用してよい。

## 8.2 到達可能性

完全ランダムにはしない。

生成時に必ず、

- 現在のプレイヤー位置
- 現在の縦速度
- タップインパルス
- 重力
- スクロール速度
- 通路幅
- 次の変化までの時間

を考慮し、理論上到達可能なコースだけを生成する。

プレイヤーの操作ミスではなく「どう操作しても無理」な地形は禁止。

---

# 9. 難易度設計

難易度は主に以下の4要素で上げる。

1. スクロール速度
2. チャートの上下変動幅
3. チャートの変化速度
4. 通路幅

## フェーズ

| DISTANCE | 難易度 | 内容 |
|---:|---|---|
| 0～200m | EASY | 緩い上下動。広い通路 |
| 200～500m | NORMAL | 上下の振れ幅増加 |
| 500～900m | VOLATILE | 急騰・急落を解禁 |
| 900～1400m | CRASH ZONE | 通路が狭くなり、高低差増加 |
| 1400m～ | ENDLESS | 全パターン混合。徐々に高速化 |

序盤の100m程度は操作を覚えるため、理不尽な急変を出さない。

---

# 10. MARKET CRASH / RALLY

高難度帯では特殊セグメントを出す。

## MARKET CRASH

チャートが大きく下方向へ移動。

出現前に約0.5～0.7秒の予兆を出す。

```text
MARKET CRASH
```

- 背景のローソク足を下落方向へ強調
- 軽い画面振動
- 通路そのものは必ず到達可能にする

## MARKET RALLY

急上昇版。

```text
MARKET RALLY
```

同様に予兆を出す。

特殊イベントは連続させず、クールダウンを設ける。

---

# 11. スコア

## 11.1 メイン記録

**DISTANCE（距離）**

を主記録とする。

例:

```text
DISTANCE  1,284m
BEST      1,562m
```

ランキングや自己ベスト比較もDISTANCEを使用する。

## 11.2 ASSET

株ゲームらしい副次表示として、仮想資産額を表示する。

開始:

```text
ASSET ¥1,000,000
```

距離とボーナスに応じて増える。

ゲームオーバー例:

```text
運用終了

DISTANCE  1,284m
ASSET     ¥3,482,100
BEST      1,562m
```

ASSETはゲーム性を複雑にしないため、ランキングの主判定には使用しない。

---

# 12. RISK BONUS

通路中央を飛ぶだけでもゲームクリアは可能。

ただし壁際を攻めた場合は追加スコアを与える。

プレイヤーと最寄りの壁との距離が、通路幅の約12%以内になった状態を `RISK ZONE` とする。

一定時間以上維持すると、

```text
RISK BONUS
+100
```

を表示。

目的:

**高リスク・高リターン**

という投資テーマを、操作にも軽く反映する。

連続加点には短いクールダウンを設け、壁際での放置によるポイント稼ぎを防止する。

---

# 13. キャラクター

既存の4キャラクターを再利用する。

能力差はゲームを壊さない程度の小さな個性に留める。

## みっちゃん — 未来予知

特徴:

**チャートの先が少し長く見える。**

通常キャラより、右側のチャートを約20～25%先まで表示する。

メリット:

- 急騰・急落を早めに判断できる
- 初見コースへの対応力が高い

## にむし — 冷静沈着

特徴:

**上下操作のレスポンスが扱いやすい。**

案:

- 重力を約5%弱くする
- タップ後の上昇速度をわずかに穏やかにする

急激な上下動が減り、細かな修正がしやすいキャラクターとする。

## ねむぱん — もちもちボディ

特徴:

**当たり判定が少し小さい。**

標準キャラ比:

```text
collisionScale = 0.90～0.93
```

狭い通路を抜けやすい。

## 女王 — 強欲

特徴:

**RISK BONUSが高い代わりに当たり判定が少し大きい。**

例:

```text
RISK BONUS ×1.5
collisionScale = 1.05～1.08
```

高リスク・高リターン型。

---

# 14. キャラクター画像

ゲーム中は通常画像を基本表示する。

プレイ中に頻繁な全身画像切り替えは行わない。

理由:

- 高速ゲームでは差分が認識しづらい
- キャラクター位置が跳ねて見える
- 操作中の視認性を優先したい

使用状態:

```text
normal
miss
```

GAME OVER時のみ `miss` 画像へ切り替える。

通常画像とミス画像の表示基準位置は同じアンカーを使用する。

---

# 15. ゲームフロー

```text
タイトル
  ↓
キャラクター選択
  ↓
操作説明
  ↓
TAP TO START
  ↓
ゲーム開始
  ↓
チャートを飛行
  ↓
徐々にボラティリティ上昇
  ↓
壁に接触
  ↓
GAME OVER
  ↓
リザルト
  ├─ RETRY
  └─ CHARACTER SELECT
```

リトライ時にタイトルへ戻さない。

**GAME OVER → RETRY → 即再開**

を最短2タップ以内にする。

---

# 16. タイトル画面

必要最低限にする。

表示:

```text
CHART RIDER

相場についていけ。

[TAP TO START]

BEST 1,562m
```

キャラクター選択への導線を下部に配置。

---

# 17. 操作説明

初回のみ簡単なチュートリアルを表示。

```text
TAP
  ↑
上昇

何もしない
  ↓
落下

チャートから
はみ出すな！
```

2回目以降は省略可能。

localStorageでチュートリアル閲覧済み状態を保存する。

---

# 18. ゲーム中UI

画面上部:

```text
DISTANCE 428m
BEST 812m
```

ASSETは小さく表示してもよい。

プレイ中は情報量を増やしすぎない。

ゲーム中はチャートとキャラクターの視認性を最優先する。

---

# 19. BEST演出

自己ベスト直前:

```text
BESTまであと100m
```

自己ベスト更新:

```text
NEW BEST!
```

1秒弱で消える。

画面中央を大きく塞がない。

---

# 20. GAME OVER

壁へ接触した瞬間:

1. 30～50ms程度のヒットストップ
2. 軽い画面振動
3. キャラクターを `miss` 差分へ変更
4. `MARKET CLOSED` または `GAME OVER`
5. リザルト表示

候補文言:

```text
MARKET CLOSED
```

```text
相場から振り落とされました。
```

```text
運用終了。
```

---

# 21. リザルト

例:

```text
MARKET CLOSED

DISTANCE
1,284m

BEST
1,562m

ASSET
¥3,482,100

[ RETRY ]
[ CHARACTER ]
```

最重要ボタンは `RETRY`。

親指で押しやすい下部中央に配置する。

---

# 22. SNS共有（任意）

MVPでは必須ではない。

追加する場合はX Web Intentを使用し、APIキーは不要。

例:

```text
📈 CHART RIDER

DISTANCE：1,284m
ASSET：¥3,482,100

相場についていけ。

#株クラRPG
```

公開URLは `location.href` ではなく、公開先確定後に定数化する。

---

# 23. 音

音なしでもゲームが成立する設計にする。

推奨SE:

- タップ: 小さな上昇音
- RISK BONUS: 軽い成功音
- MARKET CRASH: 警告音
- GAME OVER: 衝突音
- NEW BEST: 短いファンファーレ

BGMはMVPではなくてもよい。

音量OFF状態でも視覚情報だけで完全に遊べること。

---

# 24. 視覚演出

演出は短く、プレイを邪魔しない。

推奨:

- タップ時: 小さな粒子
- RISK BONUS: 壁際に光
- MARKET CRASH: 背景フラッシュ + 微振動
- NEW BEST: 文字演出
- GAME OVER: 短いシェイク

プレイ中に長いアニメーションや全画面演出は入れない。

---

# 25. スマートフォン対策

最優先:

- iPhone Safari
- Android Chrome

副対応:

- PC Chrome
- Safari
- Edge

ゲーム領域ではブラウザ標準操作の誤発火を防ぐ。

```css
#gameCanvas {
  touch-action: none;
  -webkit-user-select: none;
  user-select: none;
  -webkit-touch-callout: none;
}
```

対応項目:

- 長押し虫眼鏡 / コールアウト防止
- テキスト選択防止
- 画像長押しメニュー防止
- ダブルタップズームの誤発火防止
- ページスクロール防止
- Pull to Refreshの誤発火防止
- Safe Area対応
- Dynamic Island / ノッチ考慮

ただし制限はゲームプレイ領域に限定し、ページ全体のアクセシビリティを不必要に損なわない。

---

# 26. 画面サイズ対応

Canvas内部のゲーム座標は論理サイズで管理する。

例:

```text
logicalWidth  = 390
logicalHeight = 844
```

実画面サイズに合わせてCanvasをスケール。

当たり判定や物理値を端末の実ピクセル数へ直接依存させない。

`devicePixelRatio` を考慮して高DPI端末でも描画をぼかさない。

---

# 27. フレームレート

目標:

```text
60 FPS
```

`requestAnimationFrame()` を使用する。

物理演算はdelta timeベース。

端末性能によってゲーム速度が変わらないようにする。

極端に大きなdelta timeが来た場合は上限を設ける。

例:

```js
dt = Math.min(dt, 0.033);
```

---

# 28. バックグラウンド移行

ゲーム中に、

- ホーム画面へ移動
- タブ切り替え
- 画面ロック

などが発生した場合は自動Pause。

復帰時:

```text
TAP TO RESUME
```

を表示する。

バックグラウンド中に物理演算を進めない。

---

# 29. localStorage

保存対象:

```text
bestDistance
selectedCharacter
tutorialSeen
soundEnabled
```

ASSETの最高値は必要であれば保存する。

ランキングサーバーはMVPでは不要。

---

# 30. 技術構成

推奨:

```text
index.html
style.css
game.js
assets/
```

ビルドツールは使用しない。

Canvas 2Dでゲーム本体を描画。

DOMは、

- タイトル
- キャラ選択
- リザルト
- ボタン

などのUIに使用してもよい。

---

# 31. ゲーム状態

最低限以下の状態を持つ。

```js
const GAME_STATE = {
  TITLE: 'title',
  SELECT: 'select',
  READY: 'ready',
  PLAYING: 'playing',
  PAUSED: 'paused',
  GAME_OVER: 'game_over'
};
```

状態遷移を明確にし、ゲームロジック内で画面状態を直接乱立させない。

---

# 32. 暫定パラメータ

初期実装の目安。

```js
const CONFIG = {
  player: {
    xRatio: 0.24,
    gravity: 1.85,
    tapImpulse: -0.62,
    collisionScale: 0.82
  },

  world: {
    startScrollSpeed: 0.28,
    maxScrollSpeed: 0.52,
    speedRampDistance: 1400
  },

  corridor: {
    startWidth: 0.42,
    minWidth: 0.26,
    widthRampDistance: 1400,
    riskZoneRatio: 0.12
  },

  difficulty: {
    volatileUnlock: 200,
    crashUnlock: 500,
    narrowUnlock: 900,
    endlessUnlock: 1400
  },

  event: {
    warningSeconds: 0.6,
    specialCooldownSeconds: 6.0
  }
};
```

値は画面高・画面幅に対する比率として扱う。

---

# 33. 難易度調整の優先順位

「難しい」と感じた場合に、最初に調整する順番:

1. 通路幅
2. チャート変化の速さ
3. スクロール速度
4. 重力
5. タップインパルス

キャラの当たり判定だけを極端に広げたり狭めたりして調整しない。

ゲーム全体の基本操作感を先に整える。

---

# 34. 理不尽さ防止ルール

以下は禁止。

- 画面に見えてから反応しても絶対に間に合わない地形
- 予兆なしの垂直級急落
- プレイヤー表示より大きな当たり判定
- 通路の瞬間移動
- 特殊イベントの連続発生
- 画面外情報を知らないと回避不能な配置
- 端末性能によって難易度が変化する処理

「難しい」と「理不尽」は分離する。

---

# 35. MVP完成条件

以下を満たした時点で第一版完成とする。

- タイトル画面
- 4キャラ選択
- タップ上昇 / 無入力落下
- 横スクロール
- チャート通路生成
- 到達可能性を考慮したコース生成
- 上下壁との当たり判定
- DISTANCE表示
- BEST保存
- 難易度上昇
- MARKET CRASH / RALLY
- GAME OVER
- RETRY
- iPhone Safariで正常操作
- Android Chromeで正常操作
- ブラウザの長押し・ズーム等がゲーム中に誤発火しない

---

# 36. MVP後の追加候補

第一版が成立してから検討する。

- X共有
- デイリーランキング
- 実在指数風のステージテーマ
- NASDAQ風「高ボラモード」
- 日経風ステージ
- 暗号資産風超高難度
- キャラクター別ベスト
- 実績
- 称号
- 日替わりチャート
- ゴーストリプレイ
- スキン
- ハードモード

MVPでは追加しすぎない。

---

# 37. ゲーム設計上の最重要事項

本作の面白さは、

**チャートを眺めることではなく、値動きそのものを身体感覚で追いかけること**

に置く。

そのため、

- チャートは読みやすく
- 操作は1種類
- ルールは即理解
- 徐々に忙しく
- ミス後は即リトライ

を優先する。

最終的な理想のプレイ感:

```text
上がる！
↓
タップ

下がる！
↓
待つ

急騰！
↓
タップ、タップ

暴落！
↓
落とせ！

あと少しでBEST！
↓
死亡

……もう1回。
```

この循環が成立すれば成功。
